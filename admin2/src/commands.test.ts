import {describe,expect,it} from "vitest";
import {commandResultLabel,commandStatusLabel,hasPendingCommands} from "./commands";
import type {DeviceCommand} from "./types";
const command=(status:DeviceCommand["status"],result:Record<string,unknown>|null=null,type:DeviceCommand["command_type"]="SYNC_NOW"):DeviceCommand=>({id:"1",screen_id:"s",device_id:"d",command_type:type,status,created_at:"",delivered_at:null,completed_at:null,expires_at:"",result});
describe("command presentation",()=>{it.each([["PENDING","Aguardando Player…"],["SUCCEEDED","Concluído"],["FAILED","Falhou"],["EXPIRED","Expirado"]] as const)("maps %s",(status,label)=>expect(commandStatusLabel(status)).toBe(label));it("detects temporary polling",()=>{expect(hasPendingCommands([command("PENDING")])).toBe(true);expect(hasPendingCommands([command("DELIVERED")])).toBe(true);expect(hasPendingCommands([command("SUCCEEDED")])).toBe(false)});it("formats status and action results",()=>{expect(commandResultLabel(command("SUCCEEDED",{code:"scheduled"}))).toBe("scheduled");expect(commandResultLabel(command("SUCCEEDED",{health_state:"HEALTHY",playback_state:"PLAYING",sync_state:"OK"},"GET_STATUS"))).toBe("HEALTHY · PLAYING · OK")})});

import{describe,expect,it}from"vitest";import{canInstallUpdate,installStateLabel,installationCapabilityLabel}from"./updates";
const runtime=(extra:Record<string,unknown>={})=>({update_state:"READY_TO_INSTALL",current_version_code:14,prepared_version_code:15,...extra}) as never;
describe("OTA install presentation",()=>{it("offers install only for a newer READY artifact",()=>{expect(canInstallUpdate(runtime())).toBe(true);expect(canInstallUpdate(runtime({prepared_version_code:14}))).toBe(false);expect(canInstallUpdate(runtime({installation_state:"INSTALLING"}))).toBe(false)});it("distinguishes interactive and managed capability",()=>{expect(installationCapabilityLabel("INTERACTIVE_READY")).toContain("confirmação");expect(installationCapabilityLabel("DEVICE_OWNER")).toContain("gerenciada")});it("derives stalled verification",()=>{expect(installStateLabel("POST_UPDATE_VERIFYING",new Date(0).toISOString(),16*60_000)).toContain("Possível")});it("labels terminal states",()=>{expect(installStateLabel("INSTALLED")).toContain("concluída");expect(installStateLabel("INSTALL_FAILED")).toBe("Falhou")})});

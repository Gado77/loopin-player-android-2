import{afterEach,describe,expect,it,vi}from"vitest";import{CommandRefreshController}from"./command-refresh";
describe("pending command refresh",()=>{afterEach(()=>vi.useRealTimers());it("keeps one timer while pending then stops",async()=>{vi.useFakeTimers();const refresh=vi.fn().mockResolvedValueOnce(true).mockResolvedValueOnce(false);const c=new CommandRefreshController(refresh,()=>true,100);c.start();c.start();expect(vi.getTimerCount()).toBe(1);await vi.advanceTimersByTimeAsync(100);expect(vi.getTimerCount()).toBe(1);await vi.advanceTimersByTimeAsync(100);expect(vi.getTimerCount()).toBe(0);expect(refresh).toHaveBeenCalledTimes(2)});it("stops explicitly on logout",async()=>{vi.useFakeTimers();const refresh=vi.fn();const c=new CommandRefreshController(refresh,()=>true,100);c.start();c.stop();await vi.advanceTimersByTimeAsync(200);expect(refresh).not.toHaveBeenCalled()})});
